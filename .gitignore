# Dependencies
node_modules/

# Build outputs. The profile loads these from this directory, so they stay on
# disk; they are rebuilt from src/ and are not part of the source history.
lib/
client/*.cjs
client/*.cjs.map
agent/lib/

# Local noise
*.log
.DS_Store
